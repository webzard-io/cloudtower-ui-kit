{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "claw:build": {
      "outputs": ["dist/**"]
    },
    "generate": {
      "dependsOn": ["claw:build"],
      "outputs": ["src/generated/**"]
    },
    "build": {
      "dependsOn": ["generate"],
      "outputs": ["dist/**"]
    },
    "typings": {
      "dependsOn": ["generate"],
      "outputs": ["typings/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}
