# 组件文档模板

以下是 `packages/eagle/docs/components/<ComponentName>.md` 的标准模板。用实际内容替换尖括号占位符。

---

```markdown
# <ComponentName>

## 简介

<一两句话说明组件用途和定位。包含关键特性（如尺寸、支持的模式、核心能力）。>

## 何时使用

- <适用场景 1>
- <适用场景 2>
- <适用场景 3>

不要使用：

- <不适用场景 A> --> 请用 `<替代组件A>`
- <不适用场景 B> --> 请用 `<替代组件B>`

## 基础用法

<完整可运行的代码示例，包含 import 语句。使用 CloudTower 业务域变量名（虚拟机、集群、主机等）。>

## Props

### <自有 Props 或 Props>

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| <propName> | `<具体类型>` | `<默认值>` | <说明用途> |

### 继承的 Props

<如果继承自其他组件，说明继承来源并列出需要关注的继承属性。如无继承可删除此节。>

## 常见模式

### 模式 1：<场景名称>

<说明 + 完整代码示例。>

### 模式 2：<场景名称>

<说明 + 完整代码示例。>

## 废弃说明

当前推荐使用。

<或：已废弃，请迁移至 `<替代组件>`，迁移指南见 [migration.md](../migration.md)。>

## 相关组件

- `<组件A>`：<关系说明>
- `<组件B>`：<关系说明>
```
