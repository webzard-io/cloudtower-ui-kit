/* eslint-disable
@typescript-eslint/no-namespace
*/

import React, { useCallback, useRef } from 'react';
import { FieldState, FORM_ERROR } from '@smartx/final-form';
import {
  Form as ReactFinalForm,
  FormSpy as ReactFinalFormSpy,
} from '@smartx/react-final-form';
import * as FormKit from '../ui-kit/form';
import { antdKit, kitContext } from '../ui-kit';
import { useSelector } from 'react-redux';
import { RootState } from '../store';
import { Scalars, Maybe, <%- enums.join(', ') -%> } from './react-hooks';