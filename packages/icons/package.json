{
  "name": "@cloudtower/icons",
  "version": "0.25.8",
  "license": "MIT",
  "private": true,
  "scripts": {
    "prebuild": "rimraf ./dist",
    "build-icons": "svgo --config=svgo.yaml -f ./src/ -o ./dist --pretty --indent=2"
  },
  "devDependencies": {
    "rimraf": "^3.0.0",
    "svgo": "^1.0.0"
  },
  "exports": {
    ".": "./dist"
  }
}
