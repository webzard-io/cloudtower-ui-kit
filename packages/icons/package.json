{
  "name": "@cloudtower/icons",
  "version": "0.22.11",
  "license": "MIT",
  "private": true,
  "scripts": {
    "prebuild": "rimraf ./dist",
    "build": "svgo --config=svgo.yaml -f ./src/ -o ./dist --pretty --indent=2"
  },
  "devDependencies": {
    "rimraf": "^3.0.0",
    "svgo": "^1.0.0"
  }
}
