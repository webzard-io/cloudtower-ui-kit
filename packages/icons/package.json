{
  "name": "@cloudtower/icons",
  "version": "4100.0.17",
  "license": "MIT",
  "private": true,
  "scripts": {
    "prebuild": "rimraf ./dist",
    "build-icons": "svgo --config=svgo.yaml -f ./src/ -o ./dist --pretty --indent=2 --quiet",
    "sync-icons": "figma-export use-config .figmaexportrc.mjs"
  },
  "devDependencies": {
    "@figma-export/cli": "^6.3.0",
    "@figma-export/types": "^6.2.4",
    "rimraf": "^3.0.0",
    "svgo": "^1.0.0"
  },
  "exports": {
    ".": "./dist"
  }
}
