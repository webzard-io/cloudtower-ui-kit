import { Color } from "@src/styles/token/color";

export const StatusColorMap = {
  success: Color.fill.positive.base,
  failed: Color.fill.serious.base,
  paused: Color.fill.notice.base,
  active: Color.blue["blue-50"],
};
