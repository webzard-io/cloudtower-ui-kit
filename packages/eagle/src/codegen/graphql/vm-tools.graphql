mutation installVmtools($id: ID!) {
  installVmtools(where: { id: $id }) {
    vm_id
  }
}

mutation uninstallVmtools($id: ID!) {
  uninstallVmtools(where: { id: $id }) {
    vm_id
  }
}

mutation upgradeVmtools($id: ID!) {
  upgradeVmtools(where: { id: $id }) {
    vm_id
  }
}
